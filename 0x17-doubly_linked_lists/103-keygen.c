#include <stdio.h>

/**
 * main - it's supposed to gen a key
 * @argc: number of args
 * @argv: array of pointers to args
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	(void) argv;
	return (0);
}
